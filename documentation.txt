Create and configure database
Add guard and providers in auth.php => we will add there guards and providres for multiple authentication
after all we created 2 middlewares using this command (php artisan make:middlware AdminRedirect)(php artisan make:middlware AdminAuthenticate)
Created login controller 
created view named login
Authenticate email and password
if user want to access dashboard we redirect to login page for this we used middleware, create routes in web.php in groups 
after that we define code for guest, user in app.php file
***** Customer authentication completed *****

***** Admin authentication start ******
we created controller under admin folder named AdminController
after all code login/dashboard controller/views
we add condition in AdminRedirect middleware -> we registred this middleware in app.php we define alias
